# v1.3 后端说明文档 — 部门数据权限

## 接口

不新增接口。在 v1.0 的列表和详情查询上叠加 RuoYi 数据权限拦截。

- 复用 RuoYi 五种角色数据范围：全部、自定义部门、本部门、本部门及以下、仅本人
- 多角色取并集
- 本人创建或当前指派给自己的工单始终可见
- 无权访问统一返回"工单不存在"

## 前端

- 工单列表、详情查询无接口变更
- 列表返回的数据自动受数据权限过滤
- 用户选择器（分派、筛选等）需按部门树限制候选范围

## 规则

- 数据范围使用工单创建时的 dept_id 快照
- 用户调岗不追溯历史工单
